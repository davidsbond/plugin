// Package plugin provides the client and server implementations of a plugin. This package is only used internally
// and should never be exposed to plugin authors/users.
//
// Plugins use gRPC to provide interoperability across language implementations.
package plugin
